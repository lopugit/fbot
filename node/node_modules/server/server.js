var express = require('express')
var server = express()
    url = require('url')
    path = require('path')
    http = require('http')
    https = require('https')

//\\ requirements done

/**
 * be a bot
 */
let port = 5555
server.listen(port, 
    console.log("listening on %s", port)
)

module.exports = server